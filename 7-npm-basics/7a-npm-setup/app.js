// require first looks in nodes core modules
// but also looks for a node_modules folder in your app (...and more locations TBD)
var moment = require('moment');
console.log(moment().format('ddd, h:mA'));

// vid 73!!!
